app.controller('NavController', [
  '$scope',
  '$rootScope',
  '$state',
  'StateService',
  function(
    $rootScope,
    $scope,
    $state,
    StateService) {
    'use strict';
    console.log('#### Nav Controller');

    // Didnt end up using the nav abstract view
  }
]);
